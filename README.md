# ArrayWrap
OOP wrap for php arrays


### Usage
```php
$someArray = [  
    "name" => "John",   
    "surname" => "Doe",  
];

$wrappedArray = new ArrayWrap($someArray);
````

### Running tests
````
vendor/bin/phpunit -c phpunit.xml
````
